/**
 * Invariant companion plugin — no assertions yet.
 * The yuque-kb plugin owns no cross-package runtime invariant until the
 * sync/index pipeline exists (P3); revisit at P6.
 */
export function apply(): void {}